name: Deploy to VPS

on:
  push:
    branches:
      - master
  workflow_dispatch: # Permite execução manual

jobs:
  deploy:
    name: Deploy to Hostinger VPS
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Build CSS
        run: npx tailwindcss -i ./src/public/css/input.css -o ./src/public/css/output.css --minify

      - name: Deploy to VPS via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          source: "."
          target: ${{ secrets.VPS_DEPLOY_PATH }}
          strip_components: 0
          exclude: |
            node_modules
            .git
            .github
            .env
            *.log
            .DS_Store
            prisma/database.db
            prisma/database.db-journal
            .vscode
            .idea

      - name: Execute deployment commands on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            # Cria o diretório se não existir
            mkdir -p ${{ secrets.VPS_DEPLOY_PATH }}
            cd ${{ secrets.VPS_DEPLOY_PATH }}
            
            # Verifica se Node.js está instalado
            if ! command -v node &> /dev/null; then
              echo "❌ Node.js não está instalado. Por favor, instale Node.js 18+ na VPS."
              exit 1
            fi
            
            # Verifica se o arquivo .env existe (obrigatório)
            if [ ! -f .env ]; then
              echo "⚠️ Arquivo .env não encontrado!"
              echo "Por favor, crie o arquivo .env com as configurações necessárias antes do deploy."
              exit 1
            fi
            
            # Carrega variáveis de ambiente
            if [ -f .env ]; then
              export $(cat .env | grep -v '^#' | xargs)
            fi
            
            echo "📦 Instalando dependências..."
            # Usa npm install na primeira vez, npm ci depois
            if [ ! -d node_modules ]; then
              npm install
            else
              npm ci --production=false
            fi
            
            echo "🔧 Gerando Prisma Client..."
            npm run prisma:generate
            
            echo "🗄️ Executando migrações do banco de dados..."
            npm run prisma:migrate deploy
            
            echo "🎨 Compilando CSS..."
            npx tailwindcss -i ./src/public/css/input.css -o ./src/public/css/output.css --minify
            
            echo "🔄 Reiniciando aplicação..."
            # Opção 1: PM2 (recomendado)
            if command -v pm2 &> /dev/null; then
              if pm2 list | grep -q "app-offline"; then
                pm2 restart app-offline
                echo "✅ Aplicação reiniciada com PM2"
              else
                pm2 start server.js --name app-offline
                pm2 save
                echo "✅ Aplicação iniciada com PM2"
              fi
            # Opção 2: systemd
            elif systemctl is-active --quiet app-offline.service 2>/dev/null; then
              sudo systemctl restart app-offline
              echo "✅ Aplicação reiniciada com systemd"
            # Opção 3: Iniciar diretamente (fallback)
            else
              echo "⚠️ PM2 ou systemd não encontrado. Iniciando aplicação diretamente..."
              pkill -f "node server.js" 2>/dev/null || true
              nohup node server.js > app.log 2>&1 &
              echo "✅ Aplicação iniciada diretamente (verifique app.log para logs)"
            fi
            
            echo "🎉 Deploy concluído com sucesso!"

